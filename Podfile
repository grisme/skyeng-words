platform :ios, '11.0'
target 'SkyengWords' do
  use_frameworks!

  # Networking
  pod 'Alamofire', '~> 5.2'
  
  # UI
  pod 'SnapKit', '~> 5.0.0'
  pod 'Kingfisher', '~> 5.0'
  
  # Debugging
  pod 'Reveal-SDK', '24'
  
  target 'SkyengWordsTests' do
    inherit! :search_paths
  end

end
